collector-kick 2026-09-14T23:30:00Z
reason: canonical data is at 12,231 rows and the latest sale-notice discovery added 65 unique rows, but the current-court sweep still carries 25 deferred court/month keys and current canonical stats predate the latest current-month-only sweep fix. Start one bounded additive recovery cycle now. Preserve every canonical row and richer field. Retry official current sale notices first using the current-month-only logic; if unique discovery is zero, advance official historical sale notices, then official property search, then generic official sale-notice discovery, then official case-detail enrichment. Keep failed notice/court work in deferred/retry state instead of deleting it. Allow the separate macOS hosted rescue only when the existing health guard requests it. Never replace richer values with blanks, never delete canonical data, and never publish raw upstream payloads or material that may contain personal information. Do not bypass CAPTCHA, IP controls, access restrictions, rate limits, or other protections.