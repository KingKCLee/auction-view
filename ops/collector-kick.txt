collector-kick 2026-09-14T21:26:09Z
reason: canonical data is healthy at 12,166 rows, but the last reflected bounded collector cycle is roughly two hours old and recent current sale-notice discovery is adding zero unique rows. Start one bounded additive recovery cycle now. Preserve every canonical row and all richer existing fields. Retry official current sale notices first; when unique discovery remains zero, advance official historical sale notices, then official property search, then generic official sale-notice discovery, then official case-detail enrichment. Keep failed notice/court work in the existing deferred/retry state rather than deleting it. Allow the existing separate macOS hosted rescue only when the health guard requests it. Never replace richer values with blanks, never delete canonical data, and never publish raw upstream payloads or material that may contain personal information. Do not bypass CAPTCHA, IP controls, access restrictions, rate limits, or other protections.