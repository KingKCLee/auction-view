collector-kick 2026-09-14T15:29:00Z
reason: canonical data is healthy at 12111 rows, but the latest current sale-notice sweep, historical sale-notice backfill, and property-search cycle all added 0 unique rows while basic case-detail enrichment remains healthy. Start one bounded additive recovery cycle now. Preserve every canonical row and all richer existing fields. Retry official current sale notices first; if unique discovery remains zero, advance official historical sale notices, then official property search, then generic official sale-notice discovery, then official case-detail enrichment. If hosted Ubuntu still yields zero unique discovery, allow the existing separate macOS hosted rescue path. Never replace richer values with blanks, never delete canonical data, and never publish raw upstream payloads or material that may contain personal information. Do not bypass CAPTCHA, IP controls, access restrictions, rate limits, or other protections.