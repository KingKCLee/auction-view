name = "auction-view"
compatibility_date = "2026-01-01"
pages_build_output_dir = "public"

# The auction data the screens read. Create the namespace once with
#   npx wrangler kv namespace create AUCTION_KV
# and paste the id here (and a preview_id for `wrangler pages dev`).
[[kv_namespaces]]
binding = "AUCTION_KV"
id = "d5fe37a1d5fe403b8cacb56e2f235a30"
